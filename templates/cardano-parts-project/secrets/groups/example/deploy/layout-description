# This directory would hold deployable secrets for machines
# in the group of the parent directory name, in this case, "example"
#
# These secrets would be encrypted with sops as a binary type using an age sre/admin secret key
# and the age secret keys of the machines belonging to the group which should also have access
#
# Expect this file to generate a pre-push error until it deleted
